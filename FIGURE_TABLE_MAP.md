# Figure and table map

Approved image files are in `figures/`; all numerical replots are generated by `python -m sers.plot_results`. Schematics and publication typography are not claimed to be regenerated by that command.

| Item | Numerical source | Reproduction |
| --- | --- | --- |
| Figure 1 B–E; Table S2 | `data/figures/figure1_B.npz` through `figure1_E.npz`; `figure1_cases.csv` | Representative matched ablation curves with the original signed-trapezoidal area display normalization. `sers.ablation` runs the corresponding representative fits. Computation environments and commands are described in REPRODUCIBILITY.md. |
| Figure 2; Figure S1 | Schematic images | No experimental renderer applies. |
| Figure 3 A–E | `validation_scores.npz`, frozen pyocyanin windows, `data/figures/figure3.csv` | A is numerically generated from the W5 triplet, background, within-window ranking/retention and six-window selected ranks. B–E reproduce trajectories, entry fractions, recovery scores and spectra. Entry labels use `p_entry(m)`. |
| Figure 4 | `validation_scores.npz`, `validation_selected_vs_loss.csv`, `figure4_spectra.csv` | First-entry-region consensus count with p*, dataset-level means/SDs, and representative curves with q labels. Legacy CSV curve names map to Loss Comp. and Selected only for display. |
| Figure 5 | `test_scores.csv`, `figure5_spectra.csv` | Ad5 levels 05–07 and 04–06 plus the per-virus distributions of selected-minus-loss recovery difference across all 33 test windows. Absolute recovery values are retained in the source table. |
| Figure S2 | Network schematic | Model implementation is `sers/model.py`. |
| Figure S3 | `figureS3_overview.csv`, `figureS3_recovery.csv`, `plot_inputs/figureS3_backgrounds.csv` | Overview including the original dashed display-background curves, and before/after recovery in two output files. Pyocyanin precedes DNA/RNA, then viruses. Display-background provenance is in `metadata/plot_additions.json`; see the data dictionary for level mappings. |
| Figure S4 | `figureS4_scores.csv`, `figureS4_curves.csv`, `results/baselines/` | Baseline boxplots and representative spectra. |
| Figure S5 | `figureS5_data.csv` | Prior neural comparator diagnostic: full spectrum, 1570–1680 cm^-1 peak zoom, full absolute deviation, and zoomed deviation. |
| Figure S6 | `figureS6_data.csv`, `results/window_size/` | K = 3, 5, and 7 recovery comparisons. |
| Figure S7 | `figureS7_data.csv` | Recovery across window positions. |
| Figure S8 | `figureS8_data.csv`, baseline/test coefficient results | Coefficients across window positions. |
| Figure S9 | `figureS9.csv` | Fixed-background MCR-ALS diagnostic: full spectrum, 980–1100 cm^-1 peak zoom, full absolute deviation, and zoomed deviation. Not the superseded boxplot. |
| Figure S10 | `figureS10_data.csv`, `results/hidden_size/` | H = 16–128; median q across each dataset's validation windows for base seed 42, without an uncertainty band. H=64 is marked. The full archived result table also includes seeds 18 and 251. |
| Main Table 1; SI Table S1 | `data/grid.json` | `sers.reproduce` exports exact grid axes and selected values. |
| Main Table 2 | Full validation q matrix and grid | `sers.reproduce` exports Selected and 2nd–5th entrant with entry fractions. |
| Supporting comparator summaries | `results/baselines/baseline_task_scores.csv` | `sers.reproduce` exports per-dataset/per-method descriptive statistics. |

Experimental protocol tables are not reconstructed from model inputs. Raw instrument exports, unpublished correspondence, and manuscript files are not part of this repository.

Figure 1A and Figure S2 are conceptual network diagrams. They are supplied as
archived artwork rather than statistical outputs. Numerical replot layouts are
intentionally compact; original composite layouts remain in `figures/`.
